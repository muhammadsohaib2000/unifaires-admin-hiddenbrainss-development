export interface AppHeadInt {
  description?: string;
  title?: string;
  image?: string;
}

// eslint-disable-next-line no-unused-vars
export declare type HeadFunc = (props: AppHeadInt) => JSX.Element;
